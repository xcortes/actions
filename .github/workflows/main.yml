name: Test

on: [push]

jobs:
  test-build: 
    runs-on: ubuntu-latest
  # Como ejecutar comnados
    steps:
      - name: Checkout
        uses: actions/checkout@v4.2.1
      - name: Listar ls -l
        run: ls -l
      - name: Archivo script.sh
        rub: |
          chmod +x scrip.sh
          ./script.sh
       
      #- name: ls
      #  run: |
       #   chmod +x script.sh
        #  ./script.sh 
# | Para utilizar el scrip
#comando chmod +x es permisos de ejcución
#./scrit.sh Ejecuta dicho script