name: Test

on: [push]

jobs:
  test-build: 
    runs-on: ubuntu-latest
  # Como ejecutar comnados
    steps:
      - name: Checkout
        uses: actions/checkout@v4.2.1
       
      - name: ls
    #comando chmod +x es permisos de ejcución
    #./scrit.sh Ejecuta dicho script
        run: | # Para utilizar el scrip
          chmod +x script.sh
          ./script.sh 

